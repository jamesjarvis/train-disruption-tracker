.DS_Store
.claude/
.venv/
__pycache__/
*.pyc
secrets/
logs/
.pytest_cache/
*.egg-info/
# Machine-local rolling history (the published .ics is the durable record).
state/
# Local launchd job has machine-specific absolute paths; commit only the template.
com.bexside.disruption.plist
